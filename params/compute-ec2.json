{
  "_description": "EC2 compute instance — change the template key for different instance families: ec2-gp (M-series balanced), ec2-cpu (C-series compute), ec2-mem (R-series memory), ec2-gpu (G-series GPU)",
  "template": "ec2-gp",
  "stack_name": "REPLACE_ME-ec2",
  "parameters": {
    "ProjectName": "REPLACE_ME",
    "CostCenter": "REPLACE_ME",
    "VpcId": "vpc-REPLACE_ME",
    "SubnetId": "subnet-REPLACE_ME"
  }
}
